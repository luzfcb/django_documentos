============
Installation
============

At the command line::

    $ easy_install django_documentos

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_documentos
    $ pip install django_documentos
